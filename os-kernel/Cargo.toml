[package]
name = "os-kernel"
version = "0.1.0"
edition = "2021"
license = "MIT"

[[bin]]
name = "os-kernel"
path = "src/main.rs"

[dependencies]

[profile.release]
codegen-units = 1
lto = "fat"
opt-level = "z"
panic = "abort"
strip = true
